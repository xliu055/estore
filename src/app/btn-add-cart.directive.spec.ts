import { BtnAddCartDirective } from './btn-add-cart.directive';

describe('BtnAddCartDirective', () => {
  it('should create an instance', () => {
    // const directive = new BtnAddCartDirective();
    // expect(directive).toBeTruthy();
  });
});
